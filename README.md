# OpenClawWEB ?? ???

OpenClawWEB? Next.js 14 App Router ??? 2? ???? ?? ?? ?????????.

## ?? ??

- `??(???) -> ??(???)` ?? 2? ?? ??
- ???? STT/TTS ?? ??, ?? ? Whisper / ElevenLabs ?? ??
- ??? ?? ??, ?? ?? ?? ??, Markdown ???? ??
- ???? / ???? / ???? / ???? ? ??
- OpenClaw? ?? provider? ?? task ??? ?? ?? ??
- `apps/web`, `apps/desktop`, `packages/shared`? ?? ?? ???? ??

## ?? ?? ????

?? ?? provider ??? ?? ?????.

1. `MEETING_LLM_PROVIDER`? ???? ??? ? ?? ?? ??
2. `OPENCLAW_BASE_URL`? ??? `openclaw`? ?? ?? provider? ??
3. OpenClaw chat ?? ? `OPENAI_API_KEY -> ANTHROPIC_API_KEY -> CEREBRAS_API_KEY -> mock` ??? ??
4. ?? ??? ??? `mock` ?? provider ??

?, OpenClaw ??? GPT/OAuth? ????? ? ? `OPENAI_API_KEY` ??? ?? ??? ?? ? ????.

## ?? ??

1. `.env.local.example`? `.env.local`? ?????.
2. `npm install`? ???? ?????.
3. `npm run dev`? web SaaS ?? ?????.
4. desktop ?? ??? `npm run dev:desktop`? ?????.
5. ?????? `/meeting`? ???.

## ?? `.env.local` ?????

| ?? | ?? env | ?? env | ???? ??? ?? |
| --- | --- | --- | --- |
| ????? ??? ?? | ?? | ?? | mock ?? ??, ?? ??, ?? ????, mock OpenClaw task? ?? |
| OpenClaw? ?? ?? ?? | `OPENCLAW_BASE_URL` | `OPENCLAW_CHAT_PATH`, `OPENCLAW_API_KEY`, `OPENCLAW_API_KEY_HEADER`, `OPENCLAW_API_KEY_PREFIX`, `OPENCLAW_EXTRA_HEADERS_JSON` | ?? provider? direct LLM ?? mock?? ?? |
| direct LLM?? ?? ?? ?? | `OPENAI_API_KEY` ?? `ANTHROPIC_API_KEY` ?? `CEREBRAS_API_KEY` | `MEETING_LLM_PROVIDER`, ? provider? model env | direct provider? ??? OpenClaw ?? mock?? ?? |
| Whisper STT ?? | `OPENAI_API_KEY` | `OPENAI_MODEL` | Whisper ?? ???? |
| ???? STT/TTS ?? | ?? | ?? | env? ??, ???? ??/??? ?? ?? |
| ElevenLabs TTS ?? | `ELEVENLABS_API_KEY` | `ELEVENLABS_MODEL_ID`, `NEXT_PUBLIC_ELEVENLABS_DEFAULT_VOICE_ID` | ???? TTS? ?? |
| ?? ?? ???? ?? | `TWELVE_DATA_API_KEY` | ?? | ?? ?? ?? ???? ?? |
| ?? ?? / ?? ???? ?? | `KIWOOM_PROXY_BASE_URL` | `KIWOOM_PROXY_TOKEN` | ?? ?? ????? ?? ??? ?? |
| OpenClaw ?? task ?? | `OPENCLAW_BASE_URL` | `OPENCLAW_TASKS_PATH`, `OPENCLAW_TASK_DETAIL_PATH`, `OPENCLAW_TASK_ARTIFACTS_PATH` | ?? ??? ?? mock task ???? ?? |

## ?? ?? ??

OpenClaw??? ?? ?? + ?? task? ?? ???:

```env
OPENCLAW_BASE_URL=http://localhost:8080
OPENCLAW_CHAT_PATH=/chat
```

OpenClaw? ???? ??, ?? ? OpenAI? ????:

```env
OPENCLAW_BASE_URL=http://localhost:8080
OPENCLAW_CHAT_PATH=/chat
OPENAI_API_KEY=your_openai_api_key
```

direct OpenAI? ?? provider? ????:

```env
OPENAI_API_KEY=your_openai_api_key
MEETING_LLM_PROVIDER=openai
```

## OpenClaw API ??

? ???? ?? ???? fork ??? ?????? ?? OpenClaw? ??? ? ??? ??????.

### ?? chat ??

`POST {OPENCLAW_BASE_URL}{OPENCLAW_CHAT_PATH}`

?? ??:

```json
{
  "agentId": "analyst",
  "phase": "analysis",
  "systemPrompt": "...",
  "message": "???? ?? ???? ???",
  "history": [
    { "role": "user", "content": "???? ?? ???? ???" }
  ],
  "mode": "meeting"
}
```

?? ??:

```json
{
  "text": "## ?? ?? ??
...",
  "provider": "openclaw",
  "model": "gpt-oauth",
  "citations": [
    { "title": "Example", "url": "https://example.com" }
  ]
}
```

### ?? task ??

- `POST /tasks`
- `GET /tasks/:id`
- `GET /tasks/:id/artifacts`

? adapter? ?? alias? ?????.

- ?? ID: `taskId` | `task_id` | `id`
- ?? ID: `sessionId` | `session_id` | `session`
- ???? ID: `agentId` | `agent_id` | `agent`
- ??: `status` | `state`
- ??: `summary` | `message`
- ????: `screenshot` | `image` | `artifacts.screenshot` | `artifacts.image`
- ?? ??: `updatedAt` | `updated_at`

## ?? ???

- `POST /api/meeting/round`
- `POST /api/meeting/chat`
- `GET /api/system/capabilities`
- `POST /api/meeting/tasks`
- `GET /api/meeting/tasks/:id`
- `GET /api/meeting/tasks/:id/artifacts`

## ?? ??

- `npm run typecheck:all`
- `npm run build:web`
- `npm run build:desktop`
- `python -m py_compile apps/desktop/openclaw-backend/main.py apps/desktop/openclaw-backend/summarizer.py`

## ?? ?? ??

- ?? ??? ??? ?? ????? ?? Kiwoom ?? ???? ?????.
- ???? STT/TTS ?? ??? ?????? ????.
- OpenClaw? ?? provider? ?? task? ?? ?? ? ???, ?? ????? ? ?? ??? ??????? ????.
